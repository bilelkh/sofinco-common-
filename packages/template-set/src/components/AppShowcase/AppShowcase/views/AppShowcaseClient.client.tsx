import type { AppMobileProps } from "sofinco-react";
import { AppMobile } from "sofinco-react";

export default function AppShowcaseClient(props: AppMobileProps) {
	return <AppMobile {...props} />;
}
