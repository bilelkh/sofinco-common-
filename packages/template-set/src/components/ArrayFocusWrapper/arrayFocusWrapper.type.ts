/**
 * Server-side (edit-mode preview) prop contract for
 * `<ArrayFocusWrapperServer>`.
 */
export interface ArrayFocusWrapperServerProps {
	backgroundColor: string;
	title: string;
	subtitle: string;
	hasHeader: boolean;
}
